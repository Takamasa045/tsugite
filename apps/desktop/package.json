{
  "name": "@tsugite/desktop",
  "productName": "Tsugite",
  "version": "0.6.0",
  "description": "Tsugite local video workflow desktop application",
  "author": "Azumi Musuhi",
  "private": true,
  "type": "module",
  "main": "src/main.mjs",
  "scripts": {
    "start": "npm --prefix ../.. run build && npm --prefix ../workflow-viewer run build && electron .",
    "test": "node --test test/*.test.mjs",
    "test:packaged-workspace": "node --test test/packaged-workspace.e2e.mjs",
    "security:audit": "npm audit --omit=dev --audit-level=moderate && npm audit --audit-level=moderate",
    "prepare-runtime": "node scripts/prepare-runtime.mjs",
    "package:audit": "node scripts/audit-package.mjs",
    "build:runtime": "node scripts/clean-generated.mjs && npm --prefix ../.. run build && npm --prefix ../workflow-viewer run build && npm run prepare-runtime -- --install",
    "package": "npm run build:runtime && electron-forge package && npm run package:audit",
    "make": "npm run build:runtime && electron-forge make && npm run package:audit"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.1",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@electron-forge/cli": "7.11.2",
    "@electron-forge/maker-dmg": "7.11.2",
    "@electron-forge/maker-squirrel": "7.11.2",
    "@electron-forge/maker-zip": "7.11.2",
    "@electron/asar": "3.4.1",
    "electron": "43.1.1",
    "playwright-core": "1.61.1"
  },
  "overrides": {
    "tar": "7.5.20",
    "tmp": "0.2.7"
  }
}
