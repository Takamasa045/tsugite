name: hyperframes
capabilities:
  captions: false
  transitions: true
  audio_mix: true
  vertical: true
  fps: [24, 30]
checks:
  render_preflight:
    - name: lint
      command: ["npx", "hyperframes", "lint", "--json"]
