slug: audio-known-adapter-connection-required
run_id: audio-known-adapter-connection-required-run
manifest: ../manifests/minimal.valid.json
dist_dir: dist
edit:
  backend: remotion
audio:
  adapter: hyperframes-media
  fallback: fail
  bgm:
    id: main-bgm
    mode: generate
    prompt: Ask which service should pay before audio generation.
    start: 0
    end: 6
