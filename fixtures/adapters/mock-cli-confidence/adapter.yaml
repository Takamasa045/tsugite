name: mock-cli-confidence
kind: cli
class: analysis
offline: true
outputs: [transcript]
dry_run_estimate: true
batch: false
credit_estimate:
  per_request: 0
  per_second: 0
retry:
  max_attempts: 0
  retryable_exit_codes: []
exit_code_map:
  "0": ok
checks:
  setup: []
command:
  executable: node
  args: ["fixtures/adapters/mock-cli-confidence/analyze.mjs"]
  input: stdin-json
