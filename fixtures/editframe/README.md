# Editframe local fixture notes

Tests generate tiny H.264 fixtures at runtime. This directory does not store production projects or Gate state.

Use `npm run editframe:install` before isolated Editframe Doctor/validate or render tests. Media stays inside the run-owned directory and is referenced as `/media/<generated-name>` after copy.
