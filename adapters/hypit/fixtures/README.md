# Phase 1 labeled fixtures

No user reference video was supplied.

Live check/plan uses a byte-verified copy of the pinned
`@hypit/hypit@0.2.13` `examples/semantic-composition` tree under
`.tsugite/tools/hypit-phase1-workspace/`, marked with
`SYNTHETIC-FIXTURE.txt`.

That example is an official code-rendered chat scene. It is not
analysis of a user video. check/plan only accept exact `chat.svml` /
`chat.svrun` under that exact workspace after a byte check against the
pin. A path under the fixture is not enough. This is not a sandbox for
arbitrary Sources.
