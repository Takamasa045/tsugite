# Lessons Graduation

1. Capture a failure in `LESSONS.md` with date, symptom, cause, and rule.
2. If the rule is machine-checkable, add it to `validate` or `doctor`.
3. Add or update a test that proves the failure is rejected before execution.
4. Mark the lesson line with `validate済`.
5. If the rule is judgment-based, move it into `SKILL.md`, `CLAUDE.md`, or `AGENTS.md`.
